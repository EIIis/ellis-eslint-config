# @ellis/eslint-config

A personal reusable, professional ESLint v9 + Prettier configuration for all Next.js + TypeScript projects.

## Installation

```
pnpm add -D @ellis/eslint-config
```

## Usage

In your project’s eslint.config.js:

```
js
export { default } from "@ellis/eslint-config"
```

You now have all recommended rules for:

TypeScript

Next.js

React

Hooks

JSX Accessibility

Import sorting

Prettier formatting (automatic)
